# 知识同步与归档规范

1. 实现完成后，先从 Change 事实生成 `knowledge-sync.md` 候选；
2. 人工审核决定 apply、defer 或 reject；
3. 更新 glossary、domain、flow、decision 或 debt；
4. 完成 archive-summary；
5. 检查实验和临时生产代码；
6. 将 Change 从 active 移动到 archive；
7. 清理 `.agent-work/`。

Change 归档与知识生效是两个独立动作。
